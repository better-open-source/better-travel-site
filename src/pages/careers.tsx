import React, { FC } from 'react';
import { Layout, SEO, Stats } from '../components';

const Careers: FC = () => (
  <Layout>
    <SEO title="Careers" />
    <Stats />
  </Layout>
);

export default Careers;
